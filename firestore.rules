rules_version = '2';

// ====================================================================
// FIRESTORE SECURITY RULES - BASIC VERSION (Development Mode)
// ====================================================================
//
// ACHTUNG: Diese Rules sind OFFEN für alle!
// OK für interne Nutzung, NICHT für öffentliche Apps!
//
// DEPLOYMENT:
// firebase deploy --only firestore:rules
//
// ====================================================================

service cloud.firestore {
  match /databases/{database}/documents {

    // Alle Zugriffe erlaubt (Development Mode)
    match /{document=**} {
      allow read, write: if true;
    }

  }
}

// ====================================================================
// HINWEISE:
// ====================================================================
//
// Diese Rules sind für INTERNE Nutzung optimiert:
// - Keine Authentication erforderlich
// - Keine Validierung
// - Schnell & einfach
//
// Falls Sie später Authentication hinzufügen möchten:
// 1. Siehe DEPLOYMENT_AUTH.md (wenn vorhanden)
// 2. Oder: allow read, write: if request.auth != null;
//
// ====================================================================
